#define STREAM_LENGTH    50
#define MAX_VALUE        100
#define MIN_VALUE        0

void readFromConsole(int*);

int getMinValue(int*);

int getMaxValue(int*);

int getAverageFromLastFiveStreamValues(int*);
